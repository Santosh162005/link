services:
  - type: web
    name: content-hub
    env: node
    plan: free
    buildCommand: npm run build:deploy
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
